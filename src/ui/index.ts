import { MarketPrompt } from './marketPrompt';
import { SeedPrompt } from './seedPrompt';
import { Timer } from './timer';

const seedPrompt = new SeedPrompt();
const marketPrompt = new MarketPrompt();

export { seedPrompt, marketPrompt, Timer };
